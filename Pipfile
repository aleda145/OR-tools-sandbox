[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
ortools = "*"
black = "*"
pylint = "*"

[dev-packages]

[requires]
python_version = "3.9"

[pipenv]
# Black isnt officially released yet (lol) so need this
allow_prereleases = true
